#include <algorithm>
#include <iostream>
#include <limits>
#include <string>
#include <vector>
/*
* **离开块后继续使用局部对象的地址：指针仍保存地址，但对象已不存在，解引用是未定义行为。
* **返回局部变量的引用或指针：函数返回时局部对象销毁，调用者得到悬空引用或指针。
* **把遮蔽误当成修改外层对象：代码可以编译，但实际修改了另一个生命周期更短的对象。
* **读取未初始化的自动变量：对象虽然已进入生命周期，但值不确定；基础类型优先使用`{}`初始化。
* **误以为静态局部变量每次重置：对象一直存在，历史状态可能污染下一次测试。
*/


/*
 “地址数值还在，所以对象还在”——错误；存储位置可被保留或复用，不代表对象生命周期仍有效。
 “能编译就没有悬空”——错误；很多生命周期错误属于运行期未定义行为。
 “static让变量全局可见”——错误；静态局部变量的名字仍只在函数块内可见。
 “thread_local等于普通全局变量”——错误；它为每个线程提供独立对象。
*/

int main()
{
    return 0;
}
