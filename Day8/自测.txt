* 作用域和生命周期分别回答什么问题？
	作用域表示对象可以被调用的范围，生命周期表示对象存在的时间
* 内层同名变量会修改外层对象吗？
	不会，根据就近原则，不会修改外层对象
* 普通局部变量为什么不能通过引用安全地返回？
	因为局部变量在函数结束时就被释放
* 静态局部变量的名字和对象分别存在多久？
	名字只在作用域内可见，对象在整个程序执行期间都存在
* `thread_local`对象是一份还是每个线程一份？
	每个线程都有一份
* 动态对象和RAII今天是否计入完成要求？
	不计入