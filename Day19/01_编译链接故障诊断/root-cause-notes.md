# Day 19 故障根因记录

## 任务

- 阶段：任务一
- 第一条关键诊断：未定义标识符 "add"
- 根因：没有声明该函数
- 最小修复：增加函数声明
- 复测结果：无法解析的外部符号 "int __cdecl add(int,int)" (?add@@YAHHH@Z)，函数 main 中引用了该符号

- 阶段：任务二
- 第一条关键诊断：无法解析的外部符号 "int __cdecl add(int,int)" (?add@@YAHHH@Z)，函数 main 中引用了该符号
- 根因：只声明函数，没有对应实现
- 最小修复：增加函数具体实现
- 复测结果：

- 阶段：任务三
- 第一条关键诊断：阶段：任务二第一条关键诊断：无法解析的外部符号 "int __cdecl add(int,int)" (?add@@YAHHH@Z)，函数 main 中引用了该符号根因：只声明函数，没有对应实现最小修复：增加函数具体实现复测结果：
- 根因：声明函数与实现函数不匹配
- 最小修复：增加函数正确实现
- 复测结果：

- 阶段：任务四
- 第一条关键诊断："double __cdecl average(int,int)" (?average@@YANHH@Z) 已经在 main.obj 中定义
  "double __cdecl average(int,int)" (?average@@YANHH@Z) 已经在 main.obj 中定义
  找到一个或多个多重定义的符号
- 根因：在头文件中直接实现函数
- 最小修复：在头文件中只声明，在cpp文件中实现
- 复测结果：

- 阶段：任务五
- 第一条关键诊断：warning C4172: 返回局部变量的地址或临时 : label
- 根因：返回局部对象的引用 局部对象在作用域结束时就会销毁，会导致悬空引用
- 最小修复：不返回局部对象的引用
- 复测结果：
- 阶段：任务六
- 第一条关键诊断：未初始化变量 Sample::scaled_。始终初始化成员变量(type.6)。
- 根因：使用未初始化的raw_对象初始化scaled_ 因为 对象的初始化顺序和初始化列表顺序无关，和对象的声明顺序有关  
- 最小修复：调整对象声明时的顺序
- 复测结果：

- 阶段：任务七
- 第一条关键诊断：类 "DeviceConfig" 不存在默认构造函数
- 根因：没有默认无参构造函数，对象未初始化
- 最小修复：增加对应构造函数
- 复测结果：
- 阶段：任务八
- 第一条关键诊断：有多个 重载函数 "report" 实例与参数列表匹配:
- 根因：函数重载和默认值冲突
- 最小修复：避免让默认值和函数重载调用时产生歧义
- 复测结果：