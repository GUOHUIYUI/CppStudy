#include <iostream>


/*
	错误的引用返回 因为ref是x的引用，而x在函数结束后就被销毁了，所以返回ref会导致悬空引用，访问它会导致未定义行为。
			常见错误： 引用初始化时必须明确绑定到一个对象上，引用一旦被初始化后，就不能再指向其他对象
						返回局部变量的引用
						对引用重新赋值以为可以变更引用的绑定对象
						把引用的实现细节简单理解为指针
*/
int& broken()
{
    int x{};
	int ref = x; // ref是x的引用
    return ref;
}


int main()
{
    return 0;
}