#include <vector>

class Solution {
public:
    std::vector<std::vector<int>> generate(int numRows)
    {
        // TODO: 在LeetCode完成实现。
        (void)numRows;
        return {};
    }
};

int main()
{
    return 0;
}
