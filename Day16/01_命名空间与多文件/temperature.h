#ifndef DAY16_TEMPERATURE_H
#define DAY16_TEMPERATURE_H

// 在 units::temperature 命名空间中声明转换函数。
namespace units::temperature {
	double celsiusToFahrenheit(double celsius);
	double fahrenheitToCelsius(double fahrenheit);
}
#endif
